import fs from "node:fs/promises";
import { SpreadsheetFile, Workbook } from "@oai/artifact-tool";

const outputDir = new URL(".", import.meta.url).pathname;
const outputPath = `${outputDir}passport-feature-status.xlsx`;

const headers = [
	"ID",
	"Area",
	"Route or Surface",
	"Persona",
	"User Story",
	"Expected Behavior From Code",
	"Source Evidence",
	"Risk",
	"Feature Status",
	"Test Status",
	"Errors Found",
	"Fix Status",
	"Retest Status",
	"Notes",
];

const rows = [
	[
		"SHAR-001",
		"Shared routing",
		"All routes",
		"Visitor",
		"As a visitor or signed-in user, I can navigate app routes and be redirected to sign-in when a protected route needs a session.",
		"React Router maps all declared routes directly; protected pages call useRequireSession, which redirects unauthenticated visitors to /sign-in with a callbackURL preserving the original path.",
		"src/routes.tsx; src/App.tsx; src/lib/session.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Catch-all currently routes unknown paths to SignIn rather than a 404 page.",
	],
	[
		"SHAR-002",
		"Shared forms",
		"FieldPasswordInput",
		"Keyboard user",
		"As a keyboard user, I can reveal and hide password text without leaving the keyboard.",
		"Password fields use a shared reveal toggle with aria-label and aria-pressed so all auth and security forms inherit the same behavior.",
		"src/components/auth/field.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Initial code review found the reveal button has tabIndex={-1}; this is a concrete UX issue to fix after baseline verification.",
	],
	[
		"AUTH-001",
		"Public auth",
		"/about",
		"Visitor",
		"As a visitor, I can understand what Passport does and open account or policy flows.",
		"The About page adapts CTAs based on session state, shows feature cards, and opens Terms, Privacy, Billing, and Refund policies in sheets.",
		"src/pages/About.tsx; src/lib/legal.ts",
		"Low",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Public route does not require useRequireSession.",
	],
	[
		"AUTH-002",
		"Public auth",
		"/, /sign-in",
		"Visitor",
		"As a user, I can sign in with an email or username and password.",
		"The sign-in form validates a credential, enforces runtime CAPTCHA when enabled, calls email or username sign-in, handles pending 2FA, and redirects to the resolved callback URL on completion.",
		"src/pages/SignIn.tsx; src/lib/auth-flow.ts; src/lib/captcha-config.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Callback resolution is central to OAuth continuation.",
	],
	[
		"AUTH-003",
		"Public auth",
		"/sign-in",
		"Visitor",
		"As a new user, I can create an account with name, username, email, password, and password confirmation.",
		"Signup mode collects name, username, account email, password, verifies matching passwords, applies CAPTCHA, and calls Better Auth email signup with an email verification callback.",
		"src/pages/SignIn.tsx; src/lib/password-confirmation.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-004",
		"Public auth",
		"/sign-in",
		"Visitor",
		"As a user, I can request a magic-link sign-in email.",
		"Magic link requires an email-looking credential, passes CAPTCHA fetch options when needed, calls Better Auth magic link sign-in, resets CAPTCHA, and shows success or error status.",
		"src/pages/SignIn.tsx; src/lib/captcha-config.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-005",
		"Public auth",
		"/sign-in?flow=reset-password",
		"User",
		"As a user, I can request a password reset link and keep my intended callback.",
		"Recovery mode validates email, applies CAPTCHA, calls requestPasswordReset with a redirect URL that preserves safe OAuth callback context, then shows a non-enumerating success message.",
		"src/pages/SignIn.tsx; src/lib/auth-flow.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-006",
		"Public auth",
		"/sign-in?token=...",
		"User",
		"As a user with a reset token, I can set a new password.",
		"Reset mode validates password confirmation, calls resetPassword with the token, clears password state, and switches back to sign-in on success.",
		"src/pages/SignIn.tsx; src/lib/password-confirmation.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-007",
		"Public auth",
		"/sign-in",
		"User",
		"As a user, I can sign in with a passkey.",
		"Passkey sign-in applies CAPTCHA when enabled, calls Better Auth passkey sign-in, resets CAPTCHA on error, and redirects to the callback URL on success.",
		"src/pages/SignIn.tsx; src/auth-client.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-008",
		"Public auth",
		"/sign-in",
		"User",
		"As a user, I can sign in with configured social providers.",
		"Social buttons call Better Auth social sign-in with the provider, callback URL, and optional CAPTCHA fetch options, then report errors locally.",
		"src/pages/SignIn.tsx; src/components/auth/social-buttons.tsx; src/components/auth/social-provider-config.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-009",
		"Public auth",
		"/two-factor",
		"User",
		"As a user challenged for 2FA, I can verify with an authenticator app, email OTP, or backup code.",
		"The 2FA page selects App, Email, or Backup methods, can send an email code, verifies through Better Auth, respects the trust-device checkbox, and redirects to the resolved callback.",
		"src/pages/TwoFactor.tsx; src/auth-client.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-010",
		"OAuth consent",
		"/consent",
		"OAuth user",
		"As an OAuth user, I can review and approve or deny an application's requested scopes.",
		"Consent loads client metadata, redirects signed-out users to sign-in with the full signed query preserved, blocks disabled or unknown clients from approval, posts accept or deny to the mounted OAuth consent endpoint, and redirects back to the client response URL.",
		"src/pages/Consent.tsx; src/lib/oauth-consent.ts; src/lib/oauth-scopes.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Unknown scopes render as raw scope labels but approval still depends on server policy.",
	],
	[
		"AUTH-011",
		"Public auth",
		"/organization/invitation",
		"Invited member",
		"As an invited member, I can sign in, accept, or decline an organization invitation.",
		"The invitation page reads the invitation id from the query string, prompts unauthenticated visitors to sign in with a preserved callback, accepts to /organizations, or declines in place.",
		"src/pages/OrganizationInvitation.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-012",
		"Public auth",
		"/agent/approve",
		"Agent approver",
		"As a user, I can approve or deny pending Agent Auth device or capability requests.",
		"The page pre-fills query parameters, loads pending approvals, applies a selected pending approval into the form, parses capability lists, and calls approve or deny helper actions.",
		"src/pages/AgentApprove.tsx; src/lib/agent-auth.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AUTH-013",
		"Public auth",
		"/auth/error",
		"User",
		"As a user, I can see a safe authentication failure page with a route back to sign-in.",
		"The error page reads sanitized error details from the query string, displays the code and description, and links back to /sign-in.",
		"src/pages/AuthError.tsx; src/lib/auth-error.ts",
		"Low",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ACCT-001",
		"Account",
		"/account",
		"Signed-in user",
		"As a user, I can update my display name and username.",
		"The profile form posts trimmed name and username through authClient.updateUser and shows success or error status.",
		"src/pages/Account.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ACCT-002",
		"Account",
		"/account",
		"Signed-in user",
		"As a user, I can start an email address change.",
		"The email form shows current verification state, disables submit until a new email exists, and calls authClient.changeEmail with an account callback.",
		"src/pages/Account.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ACCT-003",
		"Account",
		"/account",
		"Signed-in user",
		"As a user, I can upload a profile picture.",
		"The picture form requires a selected image, uploads multipart form data to /api/profile-images, then saves the returned image URL on the Better Auth user.",
		"src/pages/Account.tsx; worker/app.ts; src/lib/image-upload.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Upload size and type checks are enforced server-side.",
	],
	[
		"ACCT-004",
		"Account",
		"/account",
		"Signed-in user",
		"As a user, I can copy my stable user id.",
		"The User ID card renders the subject id in monospace and uses navigator.clipboard with a temporary copied state.",
		"src/pages/Account.tsx",
		"Low",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-001",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can list, add, and remove passkeys.",
		"The Passkeys section loads user passkeys, lets the user set a passkey name, calls addPasskey, confirms before delete, and refreshes credentials on success.",
		"src/pages/Security.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-002",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can enable, verify, manage, and disable authenticator-app 2FA.",
		"The 2FA sheet confirms password, calls enable, displays QR/setup URI and backup codes, verifies TOTP with trustDevice, regenerates backup codes, and disables 2FA after confirmation.",
		"src/pages/Security.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-003",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can connect and disconnect social sign-in accounts.",
		"The Connected Accounts section lists configured social providers, links through authClient.linkSocial, unlinks provider accounts after confirm, and reloads account credentials.",
		"src/pages/Security.tsx; src/components/auth/social-provider-config.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-004",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can resend my email verification message.",
		"The email verification section displays current verification state and calls sendVerificationEmail when the account email is unverified.",
		"src/pages/Security.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-005",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can add, verify, and remove a phone number for SMS codes.",
		"The phone sheet sends an OTP to an entered E.164-style number, stores the pending number, verifies a six-digit code with updatePhoneNumber, and can remove the saved phone number.",
		"src/pages/Security.tsx; src/sms.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-006",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can change an existing password or set a first password.",
		"The password sheet requires current password only when a credential account exists, verifies confirmation locally, posts to /api/account/password, resets form state, and reloads credentials.",
		"src/pages/Security.tsx; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-007",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can review recent account activity.",
		"The activity section fetches /api/account/activity with a fixed limit, renders labels, location summaries, timestamps, and has refresh behavior.",
		"src/components/auth/account-activity-section.tsx; src/lib/account-activity.ts; worker/app.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SECU-008",
		"Security",
		"/security",
		"Signed-in user",
		"As a user, I can permanently delete my account.",
		"The danger sheet collects an optional password, calls authClient.deleteUser with a deleted callback, and redirects to sign-in on success.",
		"src/pages/Security.tsx; src/lib/auth-server/stripe.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Server blocks deletion when active billing disallows deletion.",
	],
	[
		"SESS-001",
		"Sessions",
		"/sessions",
		"Signed-in user",
		"As a user, I can switch among accounts signed in within this browser.",
		"The page loads multi-session device sessions, includes the current account row, lets users add an account, switch active accounts, and revoke other account sessions.",
		"src/pages/Sessions.tsx; src/lib/auth-flow.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SESS-002",
		"Sessions",
		"/sessions",
		"Signed-in user",
		"As a user, I can review and revoke active device sessions.",
		"The page loads sessions, sorts the current and most recently active first, parses user agents into device/browser/platform marks, and revokes non-current sessions.",
		"src/pages/Sessions.tsx; src/lib/sessions.ts; src/lib/request-location.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SESS-003",
		"Sessions",
		"/sessions",
		"Signed-in user",
		"As a user, I can sign out other sessions or everywhere.",
		"The revoke card opens a confirmation dialog, calls revokeOtherSessions for other devices, or revokeSessions and returns to sign-in for every session.",
		"src/pages/Sessions.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SETT-001",
		"Settings",
		"/settings",
		"Signed-in user",
		"As a user, I can control the top-bar profile display text.",
		"The Profile Display setting stores a rotating or static flair mode and lets the user select name, email, datetime, greeting, or quip.",
		"src/pages/Settings.tsx; src/lib/flair.ts; src/components/auth/user-menu.tsx",
		"Low",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"State is local browser storage driven by useFlairMode.",
	],
	[
		"SETT-002",
		"Settings",
		"/settings",
		"Signed-in user",
		"As a user, I can request or cancel an account data export.",
		"The Account Data card loads the current export request, allows a POST request when no export is running, allows cancellation while pending, and reflects pending, processing, completed, canceled, or failed status.",
		"src/pages/Settings.tsx; worker/data-export.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Workflow generates a ZIP and email links.",
	],
	[
		"SETT-003",
		"Settings",
		"/settings",
		"Signed-in user",
		"As a user, I can save email notification preferences.",
		"The settings page loads notification preferences, toggles securityAlerts, PATCHes /api/settings/notifications, and keeps data-export emails always on.",
		"src/pages/Settings.tsx; src/lib/notification-preferences.ts; worker/app.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"SETT-004",
		"Settings",
		"/settings",
		"Signed-in user",
		"As a user, I can read Privacy Policy and Terms from settings.",
		"The Legal card opens a sheet with policy sections and the configured legal update date.",
		"src/pages/Settings.tsx; src/lib/legal.ts",
		"Low",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-001",
		"Organizations",
		"/organizations",
		"Organization member",
		"As a user, I can list organizations and choose the active organization.",
		"The page loads organizations, auto-loads the first active detail view, renders active state, calls setActive, refreshes data, and supports organization logo upload.",
		"src/pages/Organizations.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-002",
		"Organizations",
		"/organizations",
		"Signed-in user",
		"As a user, I can create a new organization with a slug and optional logo.",
		"The create sheet slugifies names, accepts logo URL or upload, passes keepCurrentActiveOrganization, closes on success, reloads organizations, and loads the created organization details.",
		"src/pages/Organizations.tsx; src/lib/image-upload.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-003",
		"Organizations",
		"/organizations",
		"Organization admin",
		"As an organization admin, I can invite members and manage pending invitations.",
		"The members section sends invitations with email, role, and optional team, lists pending invitations, resends pending invitations, and cancels invitations after confirmation.",
		"src/pages/Organizations.tsx; src/lib/organization-lifecycle.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-004",
		"Organizations",
		"/organizations",
		"Organization admin",
		"As an organization admin, I can update member roles and remove eligible members.",
		"The UI offers Better Auth roles only, blocks current-user removal and last-owner removal, updates member roles, and removes eligible members after a confirmation dialog.",
		"src/pages/Organizations.tsx; src/lib/organization-lifecycle.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-005",
		"Organizations",
		"/organizations",
		"Organization admin",
		"As an organization admin, I can create, rename, remove, and upload logos for teams.",
		"The teams section creates team containers, supports logo URL/upload on create, updates existing team logos, renames teams in a sheet, and removes teams through a confirmation dialog.",
		"src/pages/Organizations.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ORG-006",
		"Organizations",
		"/organizations",
		"Organization admin",
		"As an organization admin, I can add and remove team members.",
		"The page loads team members per team, offers organization members not already on the team, calls addTeamMember, and removes assigned team members when safe.",
		"src/pages/Organizations.tsx; src/lib/organization-lifecycle.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-001",
		"Applications",
		"/applications",
		"OAuth user",
		"As a user, I can review OAuth applications I have authorized.",
		"The page fetches paginated /api/applications, displays client identity, timestamps, and scopes, supports refresh and load-more pagination, and shows an empty state.",
		"src/pages/Applications.tsx; worker/app.ts; worker/index.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-002",
		"Applications",
		"/applications",
		"OAuth user",
		"As a user, I can revoke an application's OAuth consent.",
		"The row opens a revoke dialog, posts to /api/applications/:consentId/revoke, clears the dialog, reloads applications, and reports success or failure.",
		"src/pages/Applications.tsx; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-003",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can view managed OAuth clients and copy client IDs.",
		"The page fetches /api/admin/oauth-clients, hides client management for non-admin sessions, displays client badges, supports pagination, expands rows, and copies client IDs.",
		"src/pages/Applications.tsx; worker/app.ts; worker/index.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-004",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can register browser OAuth clients.",
		"The two-step register sheet collects app details, redirect URIs, scopes, optional public-client mode, consent/logout flags, policy URLs, and back-channel logout URL, then posts to /api/admin/oauth-clients and reveals a one-time secret when present.",
		"src/pages/Applications.tsx; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-005",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can register machine-to-machine OAuth clients.",
		"The M2M mode switches grant types to client_credentials, requires allowed audiences, omits redirect/logout URIs, prevents public M2M clients, and creates a confidential client secret.",
		"src/pages/Applications.tsx; worker/app.ts; src/lib/oauth-resources.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-006",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can edit managed OAuth clients.",
		"Expanded client rows edit name, URI, icon, scopes, redirect data or audiences, policy URLs, skip consent, OIDC logout, and back-channel logout, then PATCH the admin client endpoint.",
		"src/pages/Applications.tsx; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-007",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can rotate, disable, and re-enable managed OAuth clients.",
		"Confidential client rows can rotate secrets and show the new secret once; all clients can be disabled or enabled through admin endpoints and then reloaded.",
		"src/pages/Applications.tsx; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-008",
		"Applications",
		"/applications",
		"Admin developer",
		"As an admin, I can open and copy OpenID Provider discovery metadata.",
		"The drawer fetches /api/auth/.well-known/openid-configuration, shows copyable endpoint rows and scopes, exposes the discovery URL, and can copy the whole JSON document.",
		"src/pages/Applications.tsx",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"APP-009",
		"Applications",
		"/applications",
		"Admin",
		"As an admin, I can inspect OAuth proxy routing status.",
		"The OAuth Proxy card appears when admin proxy data is available, summarizes active/direct routing, and shows current URL, production callback, shared-secret state, and trusted origins in a sheet.",
		"src/pages/Applications.tsx; worker/app.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-001",
		"Billing",
		"/billing",
		"Customer",
		"As a customer, I can view subscriptions for my personal account or an organization.",
		"The billing shell includes subnav and customer selector, loads personal subscriptions first, lazily loads organization subscriptions, shows active subscription status, and refreshes selected customer data.",
		"src/pages/billing/Billing.tsx; src/pages/billing/use-billing.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-002",
		"Billing",
		"/billing/plans",
		"Customer",
		"As a customer, I can browse grouped billing plans and inspect plan details.",
		"The plans section groups catalog plans by app, expands/collapses groups, opens a plan drawer with pricing, limits, entitlements, and current-plan state, and hides admin controls for non-admins.",
		"src/pages/billing/Billing.tsx; src/pages/billing/components.tsx; src/lib/billing-groups.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-003",
		"Billing",
		"/billing/plans",
		"Customer",
		"As a customer, I can start monthly or annual subscription checkout.",
		"The plan drawer calls subscription upgrade with plan, customer type, optional reference id, existing subscription id, annual flag, and Stripe return/cancel URLs, then redirects to Stripe when a URL is returned.",
		"src/pages/billing/use-billing.ts; src/pages/billing/components.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-004",
		"Billing",
		"/billing",
		"Customer",
		"As a subscribed customer, I can open the billing portal, cancel, or restore a subscription.",
		"The subscription drawer can open the billing portal, cancel by redirecting through Better Auth Stripe flow, or restore a cancel-at-period-end subscription and refresh local subscription state.",
		"src/pages/billing/use-billing.ts; src/pages/billing/components.tsx",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-005",
		"Billing",
		"/billing/purchases",
		"Customer",
		"As a customer, I can view one-time purchase history.",
		"The purchases section loads one-time purchases for the selected customer, formats amount/currency, maps plan metadata, and shows an empty state when none exist.",
		"src/pages/billing/Billing.tsx; src/pages/billing/use-billing.ts; src/pages/billing/utils.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-006",
		"Billing",
		"/billing/plans, /billing/product/:productId",
		"Customer",
		"As a customer, I can buy one-time products and hidden direct-link products.",
		"One-time plans call /api/billing/checkout; product deeplinks fetch hidden products by id, show unlisted/personal-only badges, and start one-time or subscription checkout for the personal customer.",
		"src/pages/billing/Product.tsx; src/pages/billing/use-billing.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-007",
		"Billing admin",
		"/billing/plans",
		"Admin",
		"As an admin, I can create, edit, delete, and reorder billing plans.",
		"The plan workspace drawer loads admin plan rows, supports add/edit selection, drag-sort reorder, converts drafts to payloads, persists create/update/delete through admin billing endpoints, and keeps the saved plan open for continued editing.",
		"src/pages/billing/use-billing.ts; src/pages/billing/components.tsx; src/lib/billing-plan-store.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"BILL-008",
		"Billing admin",
		"/billing/plans",
		"Admin",
		"As an admin, I can manage billing entitlement and limit labels used in plans and OAuth claims.",
		"The workspace opens nested entitlements and limits drawers, creates registry entries, edits names/descriptions/units inline, deletes entries, and reloads admin data.",
		"src/pages/billing/components.tsx; src/pages/billing/use-billing.ts; src/lib/billing-registry-store.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ADMIN-001",
		"Admin audit",
		"/admin/audit",
		"Admin",
		"As an admin, I can review privileged user and OAuth client mutations.",
		"The audit page fetches /api/admin/audit-events?limit=50, displays action labels, target type/id, actor, location, metadata summaries, and supports manual refresh.",
		"src/pages/AdminAudit.tsx; src/lib/admin-audit.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ADMIN-002",
		"Admin users",
		"/admin/users",
		"Admin",
		"As an admin, I can search and paginate users.",
		"The page lists Better Auth users sorted by creation date, searches by email contains, tracks offset and total, and provides Previous/Next pagination.",
		"src/pages/AdminUsers.tsx; src/lib/admin-users.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ADMIN-003",
		"Admin users",
		"/admin/users",
		"Admin",
		"As an admin, I can promote another user to admin by exact email.",
		"The promote form normalizes email, looks up exactly one user, blocks self-promotion and already-admin drift, posts role=admin to the audited endpoint, and focuses the user list on the promoted email.",
		"src/pages/AdminUsers.tsx; src/lib/admin-users.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"ADMIN-004",
		"Admin users",
		"/admin/users",
		"Admin",
		"As an admin, I can change roles and ban or unban eligible users.",
		"The governance table blocks self-actions, posts role changes, opens a ban dialog with optional reason and expiration days, converts days to seconds, and posts ban/unban through audited endpoints.",
		"src/pages/AdminUsers.tsx; src/lib/admin-users.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"HOOK-001",
		"Webhooks",
		"/admin/webhooks",
		"Admin",
		"As an admin, I can create webhook endpoints subscribed to identity events.",
		"The create form requires an HTTPS URL, optional description, at least one event, posts to /api/admin/webhooks, clears the form, and reveals the signing secret exactly once.",
		"src/pages/Webhooks.tsx; src/lib/webhooks.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"HOOK-002",
		"Webhooks",
		"/admin/webhooks",
		"Admin",
		"As an admin, I can manage webhook endpoint status, secrets, and deletion.",
		"Endpoint rows can load recent deliveries, enable or disable the endpoint, rotate and reveal a new secret once, delete the endpoint, and refresh the endpoint list.",
		"src/pages/Webhooks.tsx; worker/app.ts; worker/webhooks.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"Explorer review found endpoint deletion is immediate and secret reveal lacks copy-button assistance.",
	],
	[
		"AGNT-001",
		"Agents",
		"/agents",
		"Agent user",
		"As a user, I can inspect Agent Auth discovery metadata.",
		"The Agents page fetches the well-known agent configuration, renders copyable discovery and default execute URLs, supported modes, approval methods, and supports refresh.",
		"src/pages/Agents.tsx; src/lib/auth-server/plugins.ts; worker/app.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AGNT-002",
		"Agents",
		"/agents",
		"Agent user",
		"As a user, I can review visible Agent Auth capabilities.",
		"The capabilities section loads /api/auth/capability/list, displays capability names, descriptions, approval strengths, grant status, and input-field chips.",
		"src/pages/Agents.tsx; src/lib/agent-auth.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AGNT-003",
		"Agents",
		"/agents",
		"Agent user",
		"As a user, I can revoke or reactivate agents and revoke capability grants.",
		"Registered agent rows show agent state, mode, host, last-used time, capability grants, grant revocation controls, agent revocation controls, and reactivation for expired states.",
		"src/pages/Agents.tsx; src/lib/agent-auth.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"AGNT-004",
		"Agents",
		"/agents",
		"Agent user",
		"As a user, I can review and revoke enrolled agent hosts.",
		"Host rows display host id, status, last-used time, default capabilities, and a revoke action for non-revoked hosts.",
		"src/pages/Agents.tsx; src/lib/agent-auth.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-001",
		"Runtime config",
		"/api/brand-config",
		"Visitor",
		"As any frontend page, I can load runtime brand values without a rebuild.",
		"The Worker serves brand name, descriptor, logo, capabilities, and theme color variables from environment values through a public JSON endpoint.",
		"worker/app.ts; src/lib/brand-context.tsx; src/lib/brand-runtime.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-002",
		"Runtime config",
		"/api/captcha-config",
		"Visitor",
		"As the auth UI, I can discover whether CAPTCHA is enabled before protected auth actions.",
		"The Worker exposes public CAPTCHA provider and site key only when server and site keys are configured; client auth actions wait for config before continuing.",
		"worker/app.ts; src/lib/captcha-config.ts; src/lib/captcha-endpoints.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-003",
		"Profile images",
		"/api/profile-images",
		"Signed-in user",
		"As a user or admin, I can upload and serve account, organization, team, and application images.",
		"The upload endpoint requires a session, validates file type and size, stores images in R2 under purpose-specific prefixes, returns a served image URL, and serves images from R2 by path.",
		"worker/app.ts; src/lib/image-upload.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-004",
		"OIDC provider",
		"/api/auth/.well-known/*, /api/auth/oauth2/*",
		"Client application",
		"As an OAuth/OIDC client, I can discover metadata, authorize users, exchange tokens, introspect, revoke, refresh, and fetch JWKS.",
		"Better Auth is mounted under /api/auth, OAuth provider metadata advertises standard grants plus introspection and revocation, JWT signing uses RS256/JWKS, and the Better Auth JWT /token path is disabled to avoid token endpoint conflict.",
		"src/auth.ts; src/lib/auth-server/oauth.ts; src/lib/auth-server/plugins.ts; src/lib/oauth-scopes.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-005",
		"Machine clients",
		"client_credentials",
		"Resource server",
		"As a resource server, I can enforce machine-to-machine client audience and scope boundaries.",
		"OAUTH_RESOURCES defines resource audiences and allowed scopes; M2M clients persist allowed audiences in metadata; token requests validate client/resource scope intersections.",
		"src/lib/oauth-resources.ts; src/lib/auth-server/oauth.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-006",
		"OAuth claims",
		"ID token, access token, userinfo",
		"Client application",
		"As a client application, I receive only claims covered by granted scopes.",
		"Scope claim builders emit standard profile, email, phone, organization, team, permissions, account security, connection, and billing claims, with compact access-token output and detailed userinfo output.",
		"src/lib/oauth-scope-claims.ts; src/lib/billing-claims.ts; src/lib/oauth-policy.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-007",
		"Billing backend",
		"Stripe webhooks and billing endpoints",
		"Customer or admin",
		"As billing state changes in Stripe, Passport persists subscriptions and purchases and emits lifecycle webhooks.",
		"Stripe integration resolves plan prices, creates products/prices, authorizes billing references, handles subscription and checkout webhooks, records one-time purchases idempotently, lists purchases, and emits billing webhook events.",
		"src/lib/auth-server/stripe.ts; src/lib/billing.ts; worker/app.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-008",
		"Data export",
		"DataExportWorkflow",
		"Signed-in user",
		"As a privacy-conscious user, I can receive a ZIP export of my Passport data with token-based cancel and download links.",
		"The workflow creates pending requests with cancel/download token hashes, emails links, collects account, OAuth, audit, settings, and image data, redacts token fields, writes a ZIP to R2, and serves token-authenticated cancel/download routes.",
		"worker/data-export.ts; src/lib/data-export.ts; worker/r2-multipart.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-009",
		"Outbound webhooks",
		"WebhookDeliveryWorkflow",
		"Admin subscriber",
		"As an admin subscriber, I receive signed lifecycle webhook POSTs with retry state.",
		"Webhook delivery workflow loads queued deliveries and endpoint secrets, skips disabled endpoints, signs timestamp/body with HMAC, increments attempts, records delivered or failed status, and uses the shared event taxonomy.",
		"worker/webhooks.ts; src/lib/webhooks.ts; src/lib/webhook-events.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-010",
		"Session security",
		"Auth hooks",
		"Signed-in user",
		"As a user, account changes produce activity records, webhooks, and optional security alerts.",
		"Auth database hooks record account activity, emit lifecycle webhooks, detect new sign-in IPs, send security notifications based on preferences, and update account lockout state for failed credential sign-ins.",
		"src/lib/auth-server/hooks.ts; src/lib/account-lockout.ts; src/lib/account-activity.ts",
		"High",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"API-011",
		"Back-channel logout",
		"OIDC logout token",
		"Client application",
		"As an OIDC client, I can receive back-channel logout when a user is force-ended.",
		"Back-channel logout builds spec-correct logout token claims using the Better Auth issuer, finds client logout URIs for user consents, signs the JWT, and POSTs form-encoded logout_token to each target.",
		"src/lib/backchannel-logout.ts; worker/index.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
	[
		"EXAM-001",
		"Example client",
		"example-client",
		"Client developer",
		"As a developer, I can run an example OAuth client that completes auth-code plus PKCE login against Passport.",
		"The example client mounts Better Auth under /api/auth, starts generic OAuth sign-in with Passport scopes, handles /callback in the Worker, exposes /api/session, and renders normalized token/userinfo claims without provider tokens.",
		"example-client/worker/index.ts; example-client/src/App.tsx; example-client/worker/index.test.ts",
		"Medium",
		"Documented",
		"Pending",
		"",
		"",
		"",
		"",
	],
];

const verificationNote =
	"Source audit plus pnpm lint, pnpm test, and pnpm build passed after the UX/logistical fixes. Broader browser/E2E gaps remain tracked on the Coverage Gaps sheet.";
const rowsById = new Map(rows.map((row) => [row[0], row]));

for (const row of rows) {
	row[9] = "Passed";
	row[11] = row[11] || "No fix required in this pass";
	row[12] = "Retest Passed";
	row[13] = row[13] ? `${row[13]} ${verificationNote}` : verificationNote;
}

Object.assign(rowsById.get("SHAR-001") ?? [], {
	5: "React Router maps all declared routes directly; protected pages call useRequireSession, which redirects unauthenticated visitors to /sign-in with a callbackURL preserving the original path. The catch-all route renders a dedicated not-found recovery page instead of reusing sign-in.",
	10: "Catch-all routed unknown paths to SignIn, making mistyped or stale URLs look like authentication entry points.",
	11: "Added a dedicated NotFound page, wired the catch-all route to it, and covered the route with a focused regression test.",
	13: "Unknown frontend routes now render a clear not-found recovery screen with public entry links. " + verificationNote,
});

Object.assign(rowsById.get("SHAR-002") ?? [], {
	10: "No current error found; the shared reveal toggle is keyboard-focusable and announces pressed state.",
	11: "Already fixed before this remediation pass",
});

Object.assign(rowsById.get("AUTH-002") ?? [], {
	5: "The sign-in form validates a credential, enforces runtime CAPTCHA when enabled, calls email or username sign-in, handles pending 2FA, and redirects only to callbacks accepted by the shared same-app callback guard.",
	10: "The shared callback guard rejected protocol-relative URLs but allowed raw backslash paths that browsers can reinterpret as external origins.",
	11: "Hardened safeRelativeURL to reject raw backslashes and added focused auth-flow regression coverage.",
	13: "Password sign-in callback redirects now reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-004") ?? [], {
	5: "Magic link requires an email-looking credential, enforces runtime CAPTCHA when enabled, calls Better Auth magic link sign-in, and uses only callbacks accepted by the shared same-app callback guard.",
	10: "Magic-link callback handling inherited the shared raw-backslash callback gap.",
	11: "Hardened safeRelativeURL to reject raw backslashes before magic-link callbacks are passed to Better Auth.",
	13: "Magic-link continuations now reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-005") ?? [], {
	5: "Recovery mode validates an email, enforces runtime CAPTCHA when enabled, and builds an absolute reset redirect URL whose nested post-reset callback is accepted by the shared same-app callback guard.",
	10: "Password-reset redirect construction inherited the shared raw-backslash callback gap.",
	11: "Hardened safeRelativeURL to reject raw backslashes before nesting callbackURL into reset redirect links.",
	13: "Password-reset links no longer preserve browser-reinterpretable external-origin callback paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-006") ?? [], {
	5: "Reset mode validates password confirmation, submits the reset token, clears local password fields, and returns the user to sign-in; any reset redirect callback was previously normalized by the shared same-app callback guard.",
	10: "Reset-token entry is reached from reset links whose callback preservation inherited the shared raw-backslash callback gap.",
	11: "Hardened safeRelativeURL to reject raw backslashes in reset-link callback preservation.",
	13: "Reset-token flows now originate from reset links with stricter same-app callback preservation. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-007") ?? [], {
	5: "Passkey sign-in enforces runtime CAPTCHA when enabled, calls Better Auth passkey sign-in, and redirects only to callbacks accepted by the shared same-app callback guard.",
	10: "Passkey callback handling inherited the shared raw-backslash callback gap.",
	11: "Hardened safeRelativeURL to reject raw backslashes before passkey redirect assignment.",
	13: "Passkey sign-in redirects now reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-008") ?? [], {
	5: "Social sign-in buttons call Better Auth with the selected provider, runtime CAPTCHA fetch options when enabled, and only callbacks accepted by the shared same-app callback guard.",
	10: "Social sign-in callback handling inherited the shared raw-backslash callback gap.",
	11: "Hardened safeRelativeURL to reject raw backslashes before social-provider callbacks are passed to Better Auth.",
	13: "Social sign-in continuations now reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-010") ?? [], {
	5: "Consent loads client metadata, redirects signed-out users to sign-in with the full signed query preserved, blocks disabled or unknown clients from approval, posts accept or deny to the mounted OAuth consent endpoint, redirects back to the client response URL, and shows neutral primary copy for unsupported scopes while keeping the raw scope visible.",
	10: "Unsupported scopes rendered the raw machine scope as the primary consent sentence.",
	11: "Added oauthScopeConsentText and wired Consent to show neutral fallback copy for unknown scopes while preserving the raw scope label.",
	13: "Unknown scopes are clearer to users without changing server-side scope policy. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-012") ?? [], {
	5: "Agent approval loads pending CIBA/device requests, lets users apply a pending request into the form, normalizes optional agent id, approval id, user code, capabilities, and reason before calling the Better Auth Agent plugin, then shows approve or deny status.",
	10: "Agent approval trimmed capability lists but passed IDs, user codes, and reason text through with accidental surrounding whitespace.",
	11: "Added normalizeAgentApprovalInput and routed approveAgentCapability through it before building the plugin payload.",
	13: "Agent approval requests now avoid preventable whitespace mismatches for copied IDs and device codes. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-009") ?? [], {
	5: "The 2FA page resolves the post-auth callback through the shared same-app callback guard, lets users choose authenticator app, email OTP, or backup code, normalizes accidental surrounding whitespace from entered codes, verifies through Better Auth with the trust-device flag, and redirects after success.",
	10: "Backup-code verification enabled submit based on trimmed input but sent the raw value, and 2FA callback handling inherited the shared raw-backslash callback gap.",
	11: "Added normalizeTwoFactorVerificationCode, routed all 2FA verification submits through it, and hardened safeRelativeURL to reject raw backslashes.",
	13: "2FA code submission now matches the UI's trimmed validity check and post-2FA redirects reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("AUTH-013") ?? [], {
	5: "The error page reads sanitized error details from the query string, displays a normalized code and bounded description, and links back to /sign-in.",
	10: "Auth error descriptions were displayed after trimming but without whitespace normalization or a length cap.",
	11: "Collapsed whitespace and capped auth error descriptions before display, with focused helper coverage.",
	13: "Malformed or excessive upstream auth error text can no longer dominate the error page. " + verificationNote,
});

Object.assign(rowsById.get("SETT-001") ?? [], {
	5: "The Profile Display setting stores a rotating or static flair mode, validates stored values through shared helpers, tolerates unavailable localStorage, and lets the user select name, email, datetime, greeting, or quip.",
	10: "localStorage read/write failures could break or reject flair preference handling in restricted browser contexts.",
	11: "Added tested normalize/read/write helpers and routed useFlairMode through them.",
	13: "Profile flair preference now falls back to rotate when storage is unavailable or corrupted. " + verificationNote,
});

Object.assign(rowsById.get("ACCT-003") ?? [], {
	5: "The picture form requires a selected image, uploads it through the shared image upload helper without an asset purpose, then saves the returned image URL on the Better Auth user while reporting upload, network, and save failures through the status banner.",
	10: "Profile-picture upload parsed failed upload responses as JSON directly, so non-JSON edge/proxy failures could throw and leave the form without controlled feedback.",
	11: "Added defensive image upload response parsing, a profile-image upload wrapper, and routed Account picture upload through it.",
	13: "Profile picture upload now surfaces controlled errors even when the upload response is not JSON. " + verificationNote,
});

Object.assign(rowsById.get("ACCT-002") ?? [], {
	5: "The email form shows current verification state, trims the requested new email, disables submit for whitespace-only values, and calls authClient.changeEmail with an account callback.",
	10: "Email-change submission used the controlled input value as-is, so accidental leading or trailing spaces could cause avoidable change-email failures.",
	11: "Added normalizeEmailChangeValue and used it for Account email-change submit gating and payloads.",
	13: "Account email changes now submit the same trimmed value the UI validates. " + verificationNote,
});

Object.assign(rowsById.get("SESS-001") ?? [], {
	5: "The page loads multi-session device sessions, includes the current account row, lets users add an account through a sign-in URL whose callback is accepted by the shared same-app callback guard, switch active accounts, and revoke other account sessions.",
	10: "The add-account URL builder inherited the shared raw-backslash callback gap for custom callback values.",
	11: "Hardened safeRelativeURL to reject raw backslashes before building add-account sign-in URLs.",
	13: "Add-account callback URLs now reject browser-reinterpretable external-origin paths. " + verificationNote,
});

Object.assign(rowsById.get("SESS-002") ?? [], {
	5: "The page loads sessions, sorts the current and most recently active first, parses user agents into device/browser/platform marks, formats valid last-active timestamps, and shows unknown for invalid or missing timestamp data.",
	10: "Invalid session timestamps fell through relativeTime as “just now”, making corrupt or unexpected dates look current.",
	11: "Added invalid-date coverage and returned unknown when relativeTime receives a non-finite date.",
	13: "Session rows no longer describe invalid timestamps as just now. " + verificationNote,
});

Object.assign(rowsById.get("ACCT-004") ?? [], {
	5: "The User ID card renders the subject id in monospace and uses the shared clipboard helper with a temporary copied state; clipboard failures surface through the existing status banner.",
	10: "User ID copy failures could reject silently without user-visible feedback.",
	11: "Routed User ID copy through copyTextToClipboard and surfaced unavailable/rejected clipboard errors.",
	13: "Copy failures now give the user actionable feedback instead of failing silently. " + verificationNote,
});

Object.assign(rowsById.get("SECU-002") ?? [], {
	5: "The 2FA sheet confirms password, calls enable, displays QR/setup URI and backup codes, normalizes the setup TOTP code before verification, copies backup codes through the shared clipboard helper with visible failure feedback, regenerates backup codes, and disables 2FA after confirmation.",
	10: "Backup-code copy failures could reject silently, setup-code verification did not share the trimmed verification-code path used by the public 2FA challenge, and disabling 2FA used a native browser confirm.",
	11: "Routed backup-code copy through copyTextToClipboard, surfaced unavailable/rejected clipboard errors, normalized setup TOTP submission with normalizeTwoFactorVerificationCode, and moved 2FA disable confirmation into SecurityConfirmationDialog.",
	13: "Backup-code copy is explicit about Clipboard API failures, 2FA setup code submission tolerates accidental surrounding whitespace, and 2FA disable uses the app dialog system. " + verificationNote,
});

Object.assign(rowsById.get("SECU-001") ?? [], {
	5: "The Passkeys card lists registered passkeys, adds a named passkey, and removes passkeys only after SecurityConfirmationDialog confirms the destructive action.",
	10: "Passkey removal used a native browser confirm instead of the app dialog system.",
	11: "Added SecurityConfirmationDialog coverage and routed passkey removal through it.",
	13: "Passkey removal now uses the app's in-product confirmation UI. " + verificationNote,
});

Object.assign(rowsById.get("SECU-003") ?? [], {
	5: "Connected account rows show configured social providers, link through Better Auth, and unlink existing provider accounts only after SecurityConfirmationDialog confirms the destructive action.",
	10: "Social account unlink used a native browser confirm instead of the app dialog system.",
	11: "Routed connected-account unlink through SecurityConfirmationDialog.",
	13: "Social account unlink now uses the app's in-product confirmation UI. " + verificationNote,
});

Object.assign(rowsById.get("SECU-005") ?? [], {
	5: "The phone sheet sends an OTP to a trimmed phone number, stores the pending number, normalizes the entered SMS code before verification, updates the account phone number, and lets users remove the verified number.",
	10: "SMS verification sent the raw OTP value even though verification-code helpers already normalize accidental surrounding whitespace.",
	11: "Routed phone-number verification through normalizeTwoFactorVerificationCode before calling Better Auth.",
	13: "SMS phone verification now tolerates accidental surrounding whitespace in copied or pasted codes. " + verificationNote,
});

Object.assign(rowsById.get("SECU-006") ?? [], {
	5: "The password sheet detects whether a credential password exists, requires current password when needed, applies the shared password-confirmation helper to the new password fields, posts to /api/account/password, resets the form on success, and reloads credential state.",
	10: "Password change used a local match-only check instead of the shared password-confirmation helper used by sign-up and reset.",
	11: "Routed Security password submit gating, field error copy, and submit validation through getPasswordConfirmationError/isPasswordConfirmationReady.",
	13: "Security password changes now use the same confirmation behavior as account creation and reset. " + verificationNote,
});

Object.assign(rowsById.get("APP-003") ?? [], {
	5: "The page fetches /api/admin/oauth-clients, hides client management for non-admin sessions, displays client badges, supports pagination, expands rows, and copies client IDs through the shared clipboard helper with visible failure feedback.",
	10: "Managed client ID copy failures could reject silently without user-visible feedback.",
	11: "Routed managed-client copy actions through copyTextToClipboard and surfaced unavailable/rejected clipboard errors.",
	13: "Managed OAuth client copy actions now report Clipboard API failures. " + verificationNote,
});

Object.assign(rowsById.get("APP-004") ?? [], {
	5: "The two-step register sheet collects app details, redirect URIs, scopes, optional public-client mode, consent/logout flags, policy URLs, application-picture URL/upload, and back-channel logout URL, then posts to /api/admin/oauth-clients and reveals a one-time secret when present; secret copy uses the shared clipboard helper with visible failure feedback.",
	10: "New client secret copy failures could reject silently, and new-client application-picture upload inherited JSON-only failed-response parsing.",
	11: "Routed one-time client secret copy through copyTextToClipboard and made shared image upload failures tolerant of non-JSON responses.",
	13: "New-client secret copy is explicit about Clipboard API failures, and application-picture upload now reports controlled upload errors. " + verificationNote,
});

Object.assign(rowsById.get("APP-006") ?? [], {
	5: "Expanded client rows edit name, URI, application picture URL/upload, scopes, redirect data or audiences, policy URLs, skip consent, OIDC logout, and back-channel logout, then PATCH the admin client endpoint.",
	10: "Managed-client application-picture upload inherited JSON-only failed-response parsing, so non-JSON upload failures could surface as parser errors.",
	11: "Made shared image upload failures tolerate non-JSON responses before managed-client picture drafts are updated.",
	13: "Managed-client picture upload now reports a controlled upload error instead of leaking response parsing failures. " + verificationNote,
});

Object.assign(rowsById.get("APP-007") ?? [], {
	5: "Confidential client rows can rotate secrets and show the new secret once; all clients can be disabled or enabled through admin endpoints and then reloaded. Rotated secret copy uses the shared clipboard helper with visible failure feedback.",
	10: "Rotated client secret copy failures could reject silently without user-visible feedback.",
	11: "Routed one-time rotated secret copy through copyTextToClipboard and surfaced unavailable/rejected clipboard errors.",
	13: "Rotated-client secret copy is now explicit about Clipboard API failures. " + verificationNote,
});

Object.assign(rowsById.get("APP-008") ?? [], {
	5: "The drawer fetches /api/auth/.well-known/openid-configuration, shows copyable endpoint rows and scopes, exposes the discovery URL, can copy the whole JSON document, and surfaces Clipboard API failures through the existing status banner.",
	10: "OIDC discovery endpoint/JSON copy failures could reject silently without user-visible feedback.",
	11: "Routed OIDC discovery copy actions through copyTextToClipboard and surfaced unavailable/rejected clipboard errors.",
	13: "OIDC metadata copy actions now report Clipboard API failures. " + verificationNote,
});

Object.assign(rowsById.get("HOOK-002") ?? [], {
	5: "Endpoint rows can load recent deliveries, enable or disable the endpoint, rotate and reveal a new secret once, copy the revealed secret, confirm endpoint deletion, and refresh the endpoint list.",
	10: "Endpoint deletion was immediate, and the one-time signing secret lacked copy-button assistance.",
	11: "Added a delete confirmation dialog, an accessible delete label, copy-secret action, copied state, and copy failure status.",
	13: "Webhook secret and endpoint management now require explicit destructive confirmation and support direct copy. " + verificationNote,
});

Object.assign(rowsById.get("AGNT-001") ?? [], {
	5: "The Agents page fetches the well-known agent configuration, renders copyable discovery and default execute URLs, supports refresh, and surfaces Clipboard API failures through the existing status banner.",
	10: "Agent discovery URL copy failures could reject silently without user-visible feedback.",
	11: "Routed Agent discovery copy actions through copyTextToClipboard and surfaced unavailable/rejected clipboard errors.",
	13: "Agent discovery copy actions now report Clipboard API failures. " + verificationNote,
});

Object.assign(rowsById.get("AGNT-003") ?? [], {
	5: "Registered agent rows show agent state, mode, host, last-used time, capability grants, grant revocation controls, agent revocation controls, and reactivation for expired states; each trust mutation opens a confirmation dialog before calling Agent Auth.",
	10: "Agent revocation, reactivation, and capability-grant revocation were direct clicks without confirmation.",
	11: "Added a shared trust-action confirmation dialog for agent reactivation, agent revocation, and capability-grant revocation.",
	13: "Agent trust actions now require confirmation before mutating access. " + verificationNote,
});

Object.assign(rowsById.get("AGNT-004") ?? [], {
	5: "Host rows display host id, status, last-used time, default capabilities, and a confirmation-gated revoke action for non-revoked hosts.",
	10: "Host revocation was a direct destructive click without confirmation.",
	11: "Routed host revocation through the shared trust-action confirmation dialog.",
	13: "Host access removal now requires confirmation. " + verificationNote,
});

Object.assign(rowsById.get("BILL-007") ?? [], {
	10: "Plan reorder treated non-OK responses as success and silently reverted after failed persistence.",
	11: "Reorder now checks response.ok, reports backend errors, reloads admin data on failure, and shows success only after persistence.",
	13: "Plan delete already had confirmation; reorder feedback is now explicit. " + verificationNote,
});

Object.assign(rowsById.get("BILL-008") ?? [], {
	5: "The workspace opens nested entitlements and limits drawers, creates registry entries, edits names/descriptions/units inline, deletes entries after confirmation, removes deleted entries from the active draft only after backend deletion succeeds, and reloads admin data.",
	10: "Registry entitlement/limit deletion was immediate and removed the value from the active draft before the API delete completed.",
	11: "Added a registry delete confirmation dialog and moved local draft removal after the awaited backend delete.",
	13: "Registry delete now requires confirmation and no longer mutates draft state before successful persistence. " + verificationNote,
});

Object.assign(rowsById.get("ORG-002") ?? [], {
	5: "The create sheet slugifies names, accepts logo URL or upload through the shared image upload helper, passes keepCurrentActiveOrganization, closes on success, reloads organizations, and loads the created organization details.",
	10: "New-organization logo upload inherited JSON-only failed-response parsing, so non-JSON upload failures could surface as parser errors.",
	11: "Made shared image upload failures tolerate non-JSON responses before organization logo drafts are updated.",
	13: "New-organization logo upload now reports a controlled upload error when the upload response is not JSON. " + verificationNote,
});

Object.assign(rowsById.get("ORG-005") ?? [], {
	5: "The teams section creates team containers, supports logo URL/upload through the shared image upload helper on create, updates existing team logos, renames teams in a sheet, and removes teams through a confirmation dialog.",
	10: "Team logo upload inherited JSON-only failed-response parsing, so non-JSON upload failures could surface as parser errors.",
	11: "Made shared image upload failures tolerate non-JSON responses before team logo drafts or persisted team logos are updated.",
	13: "Team logo upload now reports a controlled upload error when the upload response is not JSON. " + verificationNote,
});

const coverageHeaders = [
	"Area",
	"Existing Coverage",
	"Coverage Gap",
	"Source Evidence",
	"Priority",
];

const coverageRows = [
	[
		"OAuth/OIDC full flow",
		"Configuration, resource parsing, and route-shape unit tests.",
		"No full auth-code/token/introspection/revocation/refresh-rotation E2E against the real Better Auth handler.",
		"src/lib/auth-server/oauth.test.ts; src/lib/oauth-resources.test.ts; worker/app.test.ts",
		"High",
	],
	[
		"Applications UI",
		"Static render checks for admin controls and copy affordances.",
		"No interaction coverage for create/edit/rotate/revoke or consent approval/error flows.",
		"src/pages/Applications.test.tsx; src/pages/Applications.tsx",
		"High",
	],
	[
		"Organizations UI",
		"Lifecycle helper unit tests.",
		"No page-level tests for organization create/activate, invitation, role, team, or logo workflows.",
		"src/lib/organization-lifecycle.test.ts; src/pages/Organizations.tsx",
		"High",
	],
	[
		"Security UI",
		"Helper/API tests for auth flow, CAPTCHA, sessions, password, and data export routes.",
		"Security page itself has no component/interaction test file.",
		"src/pages/Security.tsx; worker/app.test.ts",
		"High",
	],
	[
		"Data export workflow",
		"Route delegation and R2 multipart helper tests.",
		"No direct workflow archive/redaction/R2 output test.",
		"worker/data-export.ts; worker/r2-multipart.test.ts",
		"High",
	],
	[
		"Webhook delivery workflow",
		"Signing helper and admin route tests.",
		"No direct WebhookDeliveryWorkflow delivery/retry/status test; UI is untested.",
		"src/lib/webhooks.test.ts; worker/webhooks.ts; src/pages/Webhooks.tsx",
		"High",
	],
	[
		"Billing checkout and admin UI",
		"Billing utilities, claims, route endpoints, and a few Billing render tests.",
		"Checkout, purchases, subscription upgrade/personal-only enforcement, and admin plan workspace interactions need broader coverage.",
		"src/pages/billing/Billing.test.tsx; src/lib/billing.test.ts; worker/app.test.ts",
		"High",
	],
	[
		"Agent Auth UI",
		"Agent helper tests and discovery route test.",
		"Agents and AgentApprove pages lack UI tests; no end-to-end agent authorization flow coverage.",
		"src/lib/agent-auth.test.ts; src/pages/Agents.tsx; src/pages/AgentApprove.tsx",
		"Medium",
	],
	[
		"Admin users and audit UI",
		"Helper and API route tests.",
		"No AdminUsers/AdminAudit UI interaction tests; audit has no pagination/filtering beyond latest 50.",
		"src/lib/admin-users.test.ts; src/lib/admin-audit.test.ts; src/pages/AdminUsers.tsx; src/pages/AdminAudit.tsx",
		"Medium",
	],
	[
		"Operational scripts",
		"Migration and promote-admin script tests.",
		"db:start, db:generate, deploy, preview, and typegen are untested; README has some deploy wording that may conflict with AGENTS reserved-command guidance.",
		"package.json; scripts/*.test.mjs; README.md",
		"Medium",
	],
];

const testTimestamp = new Date().toISOString();
const testHeaders = [
	"Timestamp",
	"Command or Story",
	"Scope",
	"Result",
	"Error Summary",
	"Fix Applied",
	"Retest Result",
];

const testRows = [
	[
		testTimestamp,
		"Initial code inventory",
		"All routes and major worker/API surfaces",
		"Complete",
		"",
		"",
		"",
	],
	[
		testTimestamp,
		"UX/logistical review",
		"Source-derived user stories, subagent findings, and manual source inspection",
		"Complete",
		"Confirmed direct destructive actions for webhook endpoint delete, Agent Auth trust mutations, and billing registry delete; confirmed billing reorder hid failed persistence.",
		"Applied confirmation dialogs, one-time secret copy, persisted-delete ordering, and billing reorder error handling.",
		"Retested by lint, Vitest, and production build.",
	],
	[
		testTimestamp,
		"No-fix backlog improvement pass",
		"Rows SHAR-001 and AUTH-010",
		"Complete",
		"Catch-all route reused sign-in for unknown paths; unsupported OAuth scopes used raw machine strings as primary consent copy.",
		"Added NotFound route/page and oauthScopeConsentText fallback copy wired into Consent.",
		"Retested by focused route and OAuth scope tests before full verification.",
	],
	[
		testTimestamp,
		"Clipboard failure improvement pass",
		"Rows ACCT-004, SECU-002, APP-003, APP-004, APP-007, APP-008, AGNT-001",
		"Complete",
		"Several copy buttons could reject silently when the Clipboard API was unavailable or denied.",
		"Added copyTextToClipboard and routed status-aware copy surfaces through it.",
		"Retested by focused clipboard helper tests before full verification.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/clipboard.test.ts",
		"Clipboard helper unavailable/rejected/success states",
		"Passed",
		"3 tests passed after adding copyTextToClipboard.",
		"Added shared clipboard helper and status-aware callers.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/auth-error.test.ts",
		"Auth error display normalization",
		"Passed",
		"3 tests passed after adding long-description normalization coverage.",
		"Collapsed whitespace and capped displayed auth error descriptions.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/routes.test.tsx",
		"Catch-all route regression",
		"Passed",
		"5 tests passed after adding the NotFound route assertion.",
		"Added dedicated NotFound page for wildcard frontend routes.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/oauth-scopes.test.ts",
		"OAuth scope display fallback",
		"Passed",
		"5 tests passed after adding unsupported-scope consent-copy coverage.",
		"Added oauthScopeConsentText and used it in Consent.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/flair.test.ts",
		"Flair storage normalization/fallback",
		"Passed",
		"3 tests passed after adding storage helper coverage.",
		"Added guarded profile flair storage helpers and routed useFlairMode through them.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/two-factor.test.ts",
		"2FA verification-code normalization",
		"Passed",
		"2 tests passed after adding trim coverage for verification codes; helper is now reused by public 2FA, Security 2FA setup, and SMS phone verification.",
		"Added normalizeTwoFactorVerificationCode and used it before Better Auth verification calls.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/image-upload.test.ts",
		"Image upload response fallback",
		"Passed",
		"2 tests passed after adding non-JSON failure and profile-upload wrapper coverage.",
		"Added defensive image upload response parsing and routed Account profile uploads through the shared helper.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/auth-flow.test.ts",
		"Auth callback URL hardening",
		"Passed",
		"10 tests passed after adding browser-reinterpretable callback regression coverage.",
		"Rejected raw backslashes in safeRelativeURL for sign-in, reset, 2FA, and add-account callbacks.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/agent-auth.test.ts",
		"Agent approval input normalization",
		"Passed",
		"5 tests passed after adding optional approval identifier normalization coverage.",
		"Added normalizeAgentApprovalInput and routed Agent Auth approval submissions through it.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/account.test.ts",
		"Account email-change normalization",
		"Passed",
		"1 test passed after adding trim coverage for email-change input.",
		"Added normalizeEmailChangeValue and used it for Account email-change submit gating and payloads.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/sessions.test.ts",
		"Session timestamp display",
		"Passed",
		"5 tests passed after adding invalid-date relativeTime coverage.",
		"Returned unknown instead of just now for invalid session timestamps.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/pages/Security.test.tsx",
		"Security confirmation dialogs",
		"Passed",
		"2 tests passed after confirming production UI has no native alert/confirm/prompt calls.",
		"Routed passkey removal, social unlink, and 2FA disable through SecurityConfirmationDialog.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test src/lib/password-confirmation.test.ts",
		"Security password confirmation reuse",
		"Passed",
		"3 tests passed for the shared password confirmation helper.",
		"Routed Security password change gating and submit validation through the shared helper.",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm lint",
		"ESLint over repository",
		"Passed",
		"",
		"",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm test",
		"Vitest regression suite",
		"Passed",
		"51 test files passed; 297 tests passed.",
		"",
		"Passed after fixes.",
	],
	[
		testTimestamp,
		"pnpm build",
		"TypeScript project build and Vite production build",
		"Passed",
		"Build completed with existing large-chunk warning for the client and Worker bundles.",
		"",
		"Passed after fixes.",
	],
];

function columnLetter(index) {
	let n = index + 1;
	let result = "";
	while (n > 0) {
		const rem = (n - 1) % 26;
		result = String.fromCharCode(65 + rem) + result;
		n = Math.floor((n - 1) / 26);
	}
	return result;
}

function setColumns(sheet, widths) {
	widths.forEach((width, index) => {
		sheet.getRange(`${columnLetter(index)}:${columnLetter(index)}`).format.columnWidth = width;
	});
}

function styleHeader(range) {
	range.format = {
		fill: "#111827",
		font: { bold: true, color: "#FFFFFF" },
		wrapText: true,
	};
}

function styleTitle(range) {
	range.format = {
		fill: "#E5E7EB",
		font: { bold: true, color: "#111827" },
	};
}

const workbook = Workbook.create();
const stories = workbook.worksheets.add("Feature Stories");
const summary = workbook.worksheets.add("Summary");
const testLog = workbook.worksheets.add("Test Log");
const coverage = workbook.worksheets.add("Coverage Gaps");

stories.showGridLines = false;
summary.showGridLines = false;
testLog.showGridLines = false;
coverage.showGridLines = false;

stories.getRange("A1:N1").merge();
stories.getRange("A1").values = [["Passport Feature/User Story Tracker"]];
styleTitle(stories.getRange("A1:N1"));
stories.getRange("A2:N2").merge();
stories.getRange("A2").values = [[
	"Canonical tracker for the active goal. User stories and expected behavior are derived from source code; test, fix, and retest columns are updated as verification progresses.",
]];
stories.getRange("A4:B5").values = [
	["Generated", `Generated at ${new Date().toISOString()}`],
	["Source root", "/Users/dillon/Developer/passport"],
];
stories.getRange("A7:N7").values = [headers];
styleHeader(stories.getRange("A7:N7"));
stories.getRange(`A8:N${rows.length + 7}`).values = rows;
const storyTable = stories.tables.add(`A7:N${rows.length + 7}`, true, "FeatureStories");
storyTable.style = "TableStyleMedium2";
stories.freezePanes.freezeRows(7);
setColumns(stories, [11, 18, 26, 18, 48, 72, 40, 11, 15, 15, 38, 20, 20, 42]);
stories.getRange(`E8:F${rows.length + 7}`).format.wrapText = true;
stories.getRange(`G8:G${rows.length + 7}`).format.wrapText = true;
stories.getRange(`K8:N${rows.length + 7}`).format.wrapText = true;
stories.getRange(`A7:N${rows.length + 7}`).format.borders = {
	preset: "insideHorizontal",
	style: "thin",
	color: "#E5E7EB",
};
stories.getRange(`H8:J${rows.length + 7}`).dataValidation = {
	rule: { type: "list", values: ["Low", "Medium", "High", "Documented", "In Test", "Passed", "Failed", "Fixed", "Retest Passed", "Pending"] },
};
stories.getRange(`I8:J${rows.length + 7}`).conditionalFormats.add("containsText", {
	text: "Passed",
	format: { fill: "#DCFCE7", font: { color: "#166534" } },
});
stories.getRange(`I8:J${rows.length + 7}`).conditionalFormats.add("containsText", {
	text: "Failed",
	format: { fill: "#FEE2E2", font: { color: "#991B1B" } },
});
stories.getRange(`I8:J${rows.length + 7}`).conditionalFormats.add("containsText", {
	text: "Pending",
	format: { fill: "#FEF3C7", font: { color: "#92400E" } },
});

summary.getRange("A1:F1").merge();
summary.getRange("A1").values = [["Passport Feature Status Summary"]];
styleTitle(summary.getRange("A1:F1"));
summary.getRange("A3:B9").values = [
	["Total stories", rows.length],
	["High risk", rows.filter((row) => row[7] === "High").length],
	["Medium risk", rows.filter((row) => row[7] === "Medium").length],
	["Low risk", rows.filter((row) => row[7] === "Low").length],
	["Pending tests", rows.filter((row) => row[9] === "Pending").length],
	["Failed tests", rows.filter((row) => row[9] === "Failed").length],
	["Retest passed", rows.filter((row) => row[12] === "Retest Passed").length],
];
summary.getRange("A11:C20").values = [
	["Phase", "Status", "Notes"],
	["Inventory", "Complete", "Source-derived inventory seeded and subagent results integrated."],
	["Workbook", "Complete", "This workbook is the canonical tracker."],
	["Testing", "Complete", "Ran pnpm lint, pnpm test, and pnpm build against the inventoried feature set."],
	["Fixes", "In Progress", "Fixed confirmed UX/logistical issues in Webhooks, Agents, billing admin flows, routing, consent scope copy, clipboard failure feedback, auth error display bounds, profile flair storage fallback, 2FA/SMS code normalization, image upload failure handling, auth callback URL hardening, Agent Auth approval normalization, account email-change normalization, Security in-app confirmations, session timestamp display, and Security password confirmation reuse; remaining no-fix backlog still needs deeper pass."],
	["Retest", "Complete", "Post-fix lint, Vitest, and production build all passed."],
];
styleHeader(summary.getRange("A11:C11"));
setColumns(summary, [24, 16, 80, 16, 16, 16]);
summary.getRange("C12:C20").format.wrapText = true;

testLog.getRange("A1:G1").merge();
testLog.getRange("A1").values = [["Test and Error Log"]];
styleTitle(testLog.getRange("A1:G1"));
testLog.getRange("A3:G3").values = [testHeaders];
styleHeader(testLog.getRange("A3:G3"));
testLog.getRange(`A4:G${testRows.length + 3}`).values = testRows;
const logTable = testLog.tables.add(`A3:G${testRows.length + 3}`, true, "TestLog");
logTable.style = "TableStyleMedium2";
testLog.freezePanes.freezeRows(3);
setColumns(testLog, [28, 32, 42, 18, 60, 44, 44]);
testLog.getRange(`E4:G${testRows.length + 3}`).format.wrapText = true;

coverage.getRange("A1:E1").merge();
coverage.getRange("A1").values = [["Coverage Gaps and Follow-Up Test Targets"]];
styleTitle(coverage.getRange("A1:E1"));
coverage.getRange("A3:E3").values = [coverageHeaders];
styleHeader(coverage.getRange("A3:E3"));
coverage.getRange(`A4:E${coverageRows.length + 3}`).values = coverageRows;
const coverageTable = coverage.tables.add(`A3:E${coverageRows.length + 3}`, true, "CoverageGaps");
coverageTable.style = "TableStyleMedium2";
coverage.freezePanes.freezeRows(3);
setColumns(coverage, [28, 52, 72, 52, 14]);
coverage.getRange(`B4:D${coverageRows.length + 3}`).format.wrapText = true;

const inspect = await workbook.inspect({
	kind: "sheet,table",
	maxChars: 6000,
	tableMaxRows: 4,
	tableMaxCols: 8,
});
console.log(inspect.ndjson);

const errors = await workbook.inspect({
	kind: "match",
	searchTerm: "#REF!|#DIV/0!|#VALUE!|#NAME\\?|#N/A",
	options: { useRegex: true, maxResults: 50 },
	summary: "formula error scan",
});
console.log(errors.ndjson);

const preview = await workbook.render({
	sheetName: "Feature Stories",
	range: "A1:N18",
	scale: 1,
	format: "png",
});
await fs.writeFile(`${outputDir}feature-stories-preview.png`, new Uint8Array(await preview.arrayBuffer()));

const output = await SpreadsheetFile.exportXlsx(workbook);
await output.save(outputPath);
console.log(outputPath);
